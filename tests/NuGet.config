<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <!-- Tests reference source files directly and need no packages, so clear
         the inherited sources (including the BepInEx feed). Keeps `dotnet run`
         here working offline and independent of BepInEx feed availability. -->
    <clear />
  </packageSources>
</configuration>
